# CampusLink 全量复测记录（2026-09-05）

本轮按当前工作区源码重新执行测试，测试环境使用 H2 和一次性 MySQL 8.0.46 容器；MySQL 数据目录位于 tmpfs，后端与前端分别使用本机专用端口 18081、15173。测试结束后，专用服务和容器均已停止并删除。

## 结果

| 检查项 | 结果 |
| --- | --- |
| 后端 H2 完整回归 | 21/21 通过，0 失败，0 异常 |
| 后端 MySQL 8 完整回归 | 21/21 通过，0 失败，0 异常 |
| Flyway 空库迁移 | V1—V6 全部 `success=1` |
| MySQL 业务表数量 | 16 张 |
| 前端生产构建 | 通过，Vite 构建耗时 11.24 秒 |
| Playwright 端到端测试 | 8/8 通过，0 异常、0 波动、0 跳过，耗时 27.4 秒 |
| MySQL 并发结算 | 2/2 成功；发布者冻结积分 -20，两位接取者可用积分各 +10 |
| 差异格式检查 | `git diff --check` 通过，仅有 Git 的 LF/CRLF 转换提示 |

## 性能结果

性能测试使用不少于 10 万条任务和 1000 名匹配候选人，所有请求均连接一次性 MySQL 环境。完整数据见 [performance-current.json](verification-20260905/performance-current.json)。

| 场景 | 并发/请求数 | P95 | 阈值 | 失败数 |
| --- | ---: | ---: | ---: | ---: |
| 任务列表 | 50 / 500 | 232.93 ms | 1000 ms | 0 |
| 任务详情 | 50 / 200 | 111.02 ms | 2000 ms | 0 |
| 1000 候选人技能匹配 | 10 / 100 | 143.84 ms | 3000 ms | 0 |
| 申请人分页 | 10 / 100 | 38.95 ms | 2000 ms | 0 |
| 开始订单 | 10 / 10 | 49.45 ms | 2000 ms | 0 |
| 提交完成结果 | 10 / 10 | 33.97 ms | 2000 ms | 0 |
| 并发验收结算 | 10 / 10 | 407.85 ms | 2000 ms | 0 |

## 提示与测试环境说明

- H2 测试出现 Flyway 对 H2 2.3.232 的版本支持提示；测试结果不受影响，MySQL 8 空库迁移另行通过。
- Mockito 输出了未来 JDK 将限制动态加载代理的提示；当前 JDK 21 测试正常通过。
- Vite 构建提示管理端和 Element Plus 分包超过 500 kB；这是加载性能优化项，不是构建失败。
- 第一次 MySQL 启动测试时曾手动关闭演示夹具，导致测试类无法注入 `BootstrapData`，未进入业务断言。恢复测试要求的夹具配置后，全量 MySQL 测试通过。
